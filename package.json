{
  "name": "storyapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "npm start --prefix ./back-end/",
    "postinstall": "npm install --prefix ./back-end/",
    "dev": "set \"NODE_ENV=development\" && concurrently --kill-others-on-fail \"nodemon server.js --prefix ./back-end/\" \"npm start --prefix ./front-end/\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^7.2.0",
    "jsonwebtoken": "^8.5.1",
    "nodemon": "^2.0.16"
  }
}
